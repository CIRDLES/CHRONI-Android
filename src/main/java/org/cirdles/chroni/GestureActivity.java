/*package org.cirdles.chroni;

import android.app.Activity;
import android.gesture.GestureLibraries;
import android.gesture.GestureOverlayView;
import android.graphics.Color;
import android.os.Bundle;
import android.view.MotionEvent;
import android.view.View;

/**
 * Created by gabriellecozart on 4/22/15.
 *
 * This class makes the option of multiple gestures as in pinch(zoom), long touch, flick, and others
 */
/*public class GestureActivity extends Activity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        GestureOverlayView gestureOverlayView = new GestureOverlayView(this);
        View inflate = getLayoutInflater().inflate(R.layout.gesture_activity, null);
        gestureOverlayView.setGestureColor(Color.rgb(0, 225, 0));
        gestureOverlayView.setGestureVisible(true);
        gestureOverlayView.setUncertainGestureColor(Color.rgb(0, 0, 255));

        gestureOverlayView.addView(inflate);
        gestureOverlayView.addOnGestureListener(this);

        gestureLibaries gestureLib = GestureLibraries.fromRawResource(this. R.raw.gestures);


    }

    public void onGesture(GestureOverlayView arg0, MotionEvent arg1 ) {
        // TODO
    }

    public void onGestureCancelled(GestureOverlayView arg0, MotionEvent arg1) {
        // TODO
    }

    public void onGestureEnded(GestureOverlayView arg0, MotionEvent arg1) {
        // TODO
    }
}
*/